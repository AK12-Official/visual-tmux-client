## 1. Writes bound to the entry they started against

- [x] 1.1 `hub/internal/files`: capture the target's identity from the `os.Stat` the pre-transfer check already performs, carry it in `writeOptions.origin`, and re-check it in `confirmUnchanged` immediately before the rename. Verify: `TestAForcedWriteRefusesAFileReplacedDuringTheTransfer` replaces the target inside the transfer window and asserts a conflict; `TestAForcedWriteDoesNotRecreateARenamedFile` renames it and asserts not-found with no file at the original path and no staging residue.
- [x] 1.2 Apply the same check to a name that was free when the write began: a write whose `origin` is nil requires the name to still be free. Verify: `TestAWriteRefusesANameTakenWhileItWasBeingWritten` asserts a conflict and that the created file is untouched; `TestAWriteStillCreatesAFileThatStaysAbsent` asserts the create path still works.
- [x] 1.3 The identity check is unconditional (a forced write is compared against nothing but is still checked for still being the same entry), so a forced overwrite is no longer a window. Verify: the two tests above both pass a nil expectation.
- [x] 1.4 *(review round 1)* The free-name branch asks with `Lstat`, not `Stat`: a name taken by a link whose target is gone is taken, and `Stat` reports it as free — which would let the write replace the link the pre-check refuses. Verify: `TestAWriteRefusesANameTakenByADanglingLink` fails with `Stat` restored (verified by reverting) and passes with `Lstat`.

## 2. Modification times compared at full precision

- [x] 2.1 `hub/internal/files/model.go`: add `WriteResult{Mtime, MtimeNanos}` and `ExpectedMtime{Millis, Nanos *int64}` with a `matches` that uses the exact value when present and milliseconds otherwise; `Write` returns `WriteResult` and takes `*ExpectedMtime`. Verify: `TestAWriteComparesModificationTimesAtFullPrecision` asserts a conflict for an observation one nanosecond stale with the millisecond value unchanged, and a clean save with the exact value.
- [x] 2.2 `ReadResult` carries `MtimeNanos`; the transport sets `X-File-Mtime-Nanos` as a decimal string (never a JSON number) and reads `expected_mtime_nanos`; the write response carries `mtime_nanos`. Verify: `TestReadRouteExposesTheExactModificationTime`, `TestWriteRouteCarriesTheExactModificationTime`, and `TestWriteRouteBoundsTheBodyByTheFileLimit` (which asserts the string field).
- [x] 2.3 Frontend: `Stamp{millis, nanos}` in `files/api.ts`, with `nanos` never parsed; `OpenFile.mtime` becomes `OpenFile.stamp`; `writeFile` sends both and adopts what comes back. Verify: `tabs.test.ts` "a save sends the exact modification time it was given" and `api.test.ts` "readFile carries the exact modification time as an opaque string" / "writeFile adopts the exact modification time it is given back".
- [x] 2.4 A hub that reports only milliseconds still works, on both sides. Verify: `api.test.ts` "readFile tolerates a hub that reports only milliseconds"; `ExpectedMtime.matches` falls back to milliseconds when `Nanos` is nil.
- [x] 2.5 *(review round 1)* An exact time the hub would reject is not adopted, on either route: `exactNanos` accepts only a decimal count, so a malformed value costs the finer comparison instead of making every later save answer invalid-body. Verify: `api.test.ts` "an exact modification time is adopted only when it can be carried back", over eight header values including empty, non-numeric, and a decimal point.

## 3. A save answer is not recorded against a path the tab has left

- [x] 3.1 `files/tabs.ts`: `settleSave` returns the new file list and an outcome of `saved` / `moved` / `closed`, and refuses to record an answer against a tab that no longer holds the written path. Verify: `tabs.test.ts` covers all three, including that a `moved` settlement rewrites nothing about the tab.
- [x] 3.2 `FileManagerOverlay.vue`: the path the write names is passed to `settleSave` after the await rather than read again, and `moved` is reported as a warning notice. Verify: `FileManagerOverlay.dom.test.ts` "an answer for a file that has moved is not recorded either" drives the real ordering through the menu and asserts the notice names the new path. *(Unverifiable when written -- no DOM harness existed -- and closed later by the harness 10.7 added; the reason stays here because it is why 3.4 moved the rule into a module.)*
- [x] 3.3 A conflict prompt is not offered for a tab whose path moved: answering it would write to whatever is at the new name. Verify: the catch path checks the live tab's path before prompting.
- [x] 3.4 *(review round 1)* The session, the path, the text, and the expectation are captured together as one `SaveRequest` before the write travels, so the value the overlay passes at answer time is the value it captured and there is nothing to pass the wrong way round. Verify: `tabs.test.ts` "a save request captures the path it will name", plus the `moved` case driven through `beginSave`.

## 4. Reading: whole-file classification, regular files only, exact length

- [x] 4.1 Replace the sample with a streaming scan (`textScan`) that decides at the first decisive byte and carries a rune cut in half across chunk boundaries. Verify: `TestTextScanToleratesOnlyATruncatedRune`, `TestBinaryIsDecidedByTheWholeFileNotItsPrefix` (binary part past the old sample point), and `TestATextFileIsNotClassifiedByWhereItsChunkBoundariesFall`.
- [x] 4.2 Open with `O_NONBLOCK` and decide the kind from the descriptor; refuse anything that is not a regular file. Verify: `TestReadRefusesANamedPipeInsteadOfWaitingOnIt` fails on a ten-second timeout rather than passing by accident, and `TestReadRefusesADirectoryAndEveryOtherKind`.
- [x] 4.3 Bound the served length in *both* directions: a `SectionReader` over the authorized length, clamped to what the descriptor holds when that is smaller, with `X-File-Size` reporting the length actually sent. Verify: `TestReadRouteServesExactlyTheLengthItAuthorized` (growth: a ten-byte descriptor with an authorized size of four) and `TestReadRouteServesAShortenedFileAtItsNewLength` (shrink: verified by reverting the clamp, which fails it with `Content-Length: 10`).

## 5. Listings bounded by the listing bound

- [x] 5.1 `readListing` reads `limit + 1` entries in batches with a context check between them; `List` sorts explicitly (directories first, then by name) rather than relying on the reader's order. Verify: `TestReadListingStopsOneEntryPastTheBound`, `TestReadListingReportsNoTruncationForADirectoryThatFits`, `TestReadListingStopsWhenTheCallerIsGone`, and the pre-existing ordering and truncation tests.
- [x] 5.2 Describe a symbolic link by its target, resolved through the guard so a link out of the boundary is not described by it. Verify: `TestListReportsALinkToADirectoryAsADirectory` (including that a link to a file reports the target's size) and `TestListDoesNotFollowALinkOutOfTheBoundary`. Both assert the entries were found, so a listing that returned nothing cannot pass them by iterating an empty slice.
- [x] 5.3 *(review round 1)* Apply the staging-file skip inside the read rather than to the result, so the bound counts entries the caller will be shown: filtering afterwards made a directory holding exactly the bound report itself truncated while a write to it was in flight. Verify: `TestAWriteInFlightDoesNotMakeAListingLookTruncated` drives a real in-flight write and asserts the flag stays false and all three entries are shown.

## 6. The pane working directory is returned as the pane holds it

- [x] 6.1 `hub/internal/tmux/panes.go`: `strings.TrimSpace` becomes `strings.TrimSuffix(s, "\n")`, so only the terminator tmux appends is removed. Verify: `TestPaneWorkingDirectoryKeepsWhitespaceInTheName` (trailing space, leading space, tab, internal runs, a name spanning two lines) and `TestPaneWorkingDirectoryKeepsANameThatIsOnlyWhitespace`.
- [x] 6.2 Pin the assumption the fix rests on — that tmux writes the directory and exactly one newline — against a real server. Verify: `TestPaneWorkingDirectoryKeepsATrailingSpaceAgainstARealServer` creates a session in a directory whose name ends with a space and asserts the path comes back byte-exact.

## 7. Editor state survives switching files

- [x] 7.1 `FileManagerOverlay.vue` keeps every editable tab's `CodeEditor` mounted and shows only the active one, keyed by tab id so a rename keeps the instance. Verify: **half machine-verified.** `FileManagerOverlay.dom.test.ts` "every open file keeps an editor, and only the active one is shown" asserts one mounted editor per open file, that the inactive one is hidden rather than destroyed, and that switching between them destroys neither; "closing a tab releases its editor, and switching between them does not" asserts the close destroys exactly one. The *rename keeps the instance* half is covered by no test. *(Closed by a later round: `FileManagerOverlay.dom.test.ts` "a rename keeps the editor that is holding the file" renames a file with an editor open and asserts that no new instance was built, that the one there was is not destroyed, and that what had been typed into it is still in it -- which fails with the editor keyed by path instead of by session, verified by reverting.)*
- [x] 7.2 `CodeEditor.vue` gains an `active` prop and requests a measure when it becomes visible, since an editor laid out while hidden has no dimensions. Verify: **not machine-verified, and cannot be here** -- jsdom lays nothing out, so a measure that works and one that does not are indistinguishable in it. It needs a browser -- so it is recorded as an accepted item in `design.md` rather than left as an unfinished task, which is what this branch does with a claim no test here can hold. *(Review round 1 removed an `immediate: true` on that watcher: it runs during setup, before `onMounted` creates the view, so it was dead code whose comment described a case it could not cover.)*
- [x] 7.3 The "which tabs the editor holds" rule is `editable`/`presentation` in `files/preview.ts`, testable without a DOM and reachable from `files/tabs.ts` without importing the renderer. Verify: `preview.test.ts` "the editor holds source, and nothing it must not render as text" and "presentation follows the hub over the file name".

## 8. Boundary claims match the implementation

- [x] 8.1 `openspec/specs/file-manager/spec.md`: state what `roots` does and does not guarantee, with a scenario for a caller naming a path that resolves outside every root and one for another local process replacing a path component. Verify: read the "Filesystem access boundary" requirement. *(Review round 1: the first wording contradicted the `SHALL` it sat beside and described the affected caller as "one who holds the token and nothing else" — a set the README's own reasoning shows is empty, since a token also authorizes a terminal.)*
- [x] 8.2 `README.md` and `README.zh-CN.md`: the same statement in operator-facing words, plus the listing bound, the whole-file classification, the regular-files-only rule, and the commit-time identity check. Verify: read the "File access" sections of both; they are paragraph-for-paragraph equivalent.
- [x] 8.3 Record the `os.Root` migration as the follow-up, with the probe results that ruled it out for this change. Verify: `design.md` decision 1.

## 9. Verification

- [x] 9.1 `cd hub && go test ./... && go vet ./...` pass.
- [x] 9.2 `npm --prefix hub/web test` passes, and `npm --prefix hub/web run build` (which is `vue-tsc --noEmit` plus a Vite build) succeeds.
- [x] 9.3 `make lint` passes (golangci-lint config verify plus a run over `./...`).
- [x] 9.4 Independent review of the diff by four subagents that did not write it, split across disjoint file sets (Go service; Go transport and tmux; frontend; claims, tests and docs), with each finding reproduced before it was acted on. Findings and their disposition are in `review.md`.
- [x] 9.5 A second round: closure verification of every round-1 finding (21 of 22 closed; the six with a test to revert were each confirmed by reverting it and watching the named test fail) plus adversarial re-review of the fixes themselves, split across Go and frontend. Verify: `review.md` round 2. It found one new defect — the exact modification time was validated by shape and not by whether the hub can parse it — which 9.6 fixes.
- [x] 9.6 *(review round 2)* Bound the exact modification time to what a signed 64-bit integer holds, and the millisecond value to a safe integer. The two bounds answer different failures: the first is the range the hub's integer parse accepts, the second the range a double returns unchanged. Verify: the `api.test.ts` case table includes both int64 bounds as accepted and two overflowing values as rejected, and "readFile adopts only a millisecond time it can send back" covers the fractions and exponents.

## 10. Second review round

- [x] 10.1 `os.Root` confinement for the configured case: `Confined` in `hub/internal/files/confine.go`, opened handles on each root, and every operation routed through it, with `escapedWithin` mapping the unexported escape error to not-found. Verify: the whole pre-existing symlink and dangling-link suite passes unchanged, `TestAConfinedPathIsRefusedForLeadingOutOfItsRoot` shows a plain call on the same spelling following the link while the confined call refuses it, and `TestARootedListingStillHidesAStagingFile` pins the staging key across the two forms of the path.
- [x] 10.2 The save/rename crossing: `settleSave` takes whether a rename of the written path is outstanding and reports `raced` instead of recording the answer; the overlay tracks unanswered renames. Verify: `tabs.test.ts` covers the outcome and its precedence, and `FileManagerOverlay.dom.test.ts` drives the real ordering through the menu — reverting the fourth argument fails exactly that test.
- [x] 10.3 The image preview bound applies to the bytes: `readFileBytes(path, limit)` refuses a response over the bound before reading its body, and `ImagePreview` reports the real size so the tab presents as information. Verify: `api.test.ts` covers both outcomes; the component test asserts two editors, one visible.
- [x] 10.4 A file whose name says binary is put to the hub: `probeFile` issues a bodyless HEAD on the read route and the answer decides whether the editor opens. Verify: `api.test.ts` for the request and the headers, and the overlay routes `info` files through it.
- [x] 10.5 The listing's metadata phase checks the context. Verify: the loop in `List`; the existing cancellation test covers the read phase.
- [x] 10.6 The classification-and-body race is recorded rather than fixed, with its bounded harm (`design.md`, risks), and stated in the spec under Bounded file reading.
- [x] 10.7 The DOM harness: `jsdom` and `@vue/test-utils` as devDependencies, `test/vue-loader.mjs`, `test/dom.mjs`, and the editor stub extended to apply a change and call its listener. Verify: `npm test` runs 138 tests including four mounted ones, and `npm run build` still passes.

## 11. Second review of the change

- [x] 11.1 The boundary is enforced through an `os.Root` handle rather than checked against a string, for the configured case only. Verify: the pre-existing symlink and dangling-link suite passes unchanged, `TestAConfinedPathIsRefusedForLeadingOutOfItsRoot` shows a plain call following the link that the confined call refuses, and the rooted operations each have a test of their own.
- [x] 11.2 A save answer that crossed a rename of the same path is not recorded. Verify: `tabs.test.ts` for the outcome and its precedence, and `FileManagerOverlay.dom.test.ts` for the ordering driven through the menu — reverting the flag fails exactly that test.
- [x] 11.3 The image bound is applied to the length the hub reports at read time, and a file over it is presented as information with the reason. Verify: `api.test.ts` for both outcomes and the absent-length case; the component test drives the event and the handler, and reverting the handler fails it.
- [x] 11.4 A file whose name says binary is put to the hub before the browser decides. Verify: the component test asserts the `HEAD` was issued and the editor rendered; removing the probe block fails it.
- [x] 11.5 The listing's metadata phase is cancellable. Verify: the loop in `List`.
- [x] 11.6 The classification is three-valued, so "the hub read this as text" and "nobody read this" are distinguishable. Verify: `preview.test.ts` covers all three states, and collapsing them back fails the component test that pins the probe path.
- [x] 11.7 A DOM test harness: `jsdom` and `@vue/test-utils`, an SFC loader for `node:test`, and a document. Verify: 145 frontend tests including nine mounted ones, and the build still passes.
- [x] 11.8 The classification-and-body race, the root-of-`/` case, and the move between roots are recorded as residuals with their harm. Verify: `design.md`, risks.

## 12. Third review round

- [x] 12.1 A move between two configured roots is refused rather than performed on the two absolute paths, with an error of its own (`ErrCrossRoot`, `cross_root_move` on the wire) because neither path is outside the boundary. With roots configured, none of the `c.root == nil` branches in `confine.go` is reachable any more, and `renameAt` is the one that could have been. Verify: `TestAMoveBetweenTwoRootsIsRefused` asserts the error, that it is *not* the not-allowed one, and that neither entry moved, and fails with `renameAt`'s refusal reverted to `os.Rename` (verified by reverting); the transport table gained the `cross_root_move` row.
- [x] 12.2 The hub gives its root handles back when it stops: `Service.Close` releases them, `App` keeps the service, and `Shutdown` closes it after the HTTP server has stopped. Verify: `TestClosingAServiceReleasesItsRootHandles` asks the descriptor directly and finds it closed, and fails when `Close` only sets the flag (verified by reverting); `TestShutdownReleasesTheConfiguredRoots` drives a real `App` through shutdown and asserts the set refuses afterwards, and fails with the close removed from `Shutdown` (verified by reverting).
- [x] 12.3 The browser does not let its own deletes race its own saves: a delete waits for the writes already travelling for the entry or anything beneath it, and a save whose path has a delete in flight is refused rather than sent. Verify: `FileManagerOverlay.dom.test.ts` "a delete is not sent while a save of the same file is travelling" and "a save is refused while the delete of that file is in flight" -- both confirmed to fail with their fix reverted.
- [x] 12.4 The outstanding-operation records are counts, not flags: two of a kind can be in flight on one path, and the first answer must not clear the second's record. `files/pending.ts` holds the rule for renames and saves alike, with the wait a delete needs. Verify: `pending.test.ts` covers the counting rule directly, and the component test "one of two renames being answered does not clear the other" fails with flag semantics restored (verified by reverting).
- [x] 12.5 An image within the bound is presented as an image whatever the hub classified its contents as, because the classification answers whether bytes may be decoded as text and the image preview never decodes anything. Verify: `preview.test.ts` "an image within the bound is shown as an image, binary or not" and the mounted test "an image that has shrunk below the bound is previewed as an image", both confirmed to fail against the old rule.
- [x] 12.6 The specification, both READMEs and `design.md` state the refusals that replaced the round-2 exception, and the residual list no longer names a cross-root move as an exception that is performed. Verify: `openspec validate 2026-09-16-file-manager-cr-hardening --strict`; `design.md` decisions 1, 1a, 1b, 5a and 6.
- [x] 12.7 The classification-and-body race stays recorded rather than fixed, with the reason the obvious mitigation is not taken: a post-scan re-check cannot tell an append from an in-place rewrite, and refusing on either would break reading a log being written to. Verify: `design.md`, risks.

## 13. The review of the third report's fixes

- [x] 13.1 *(review round 1)* `RootSet.Close` is synchronized against `Confine`: closing became a production path when `Shutdown` started calling it, and an unsynchronized reader could index a slice the closer had emptied — a panic, not a refusal. Verify: `TestClosingARootSetRacesItsReadersWithoutPanicking` fails with `WARNING: DATA RACE` under `-race` with the lock removed (verified by reverting). `fs.ErrClosed` from a handle closed underneath an operation now maps to the same not-allowed refusal rather than reaching the transport's fallback.
- [x] 13.2 *(review round 1)* The delete waits by **path**, not by the tabs it is about to close, and the record covers a subtree. The first version waited on `doomed`, and a write outlives its tab: saving a file and closing the tab left the write travelling with nothing for the delete to find it by. Verify: `FileManagerOverlay.dom.test.ts` "a delete waits for a write whose tab has already been closed" and "a delete of a directory waits for a write inside it" — both fail against the tab-based wait (verified by reverting). `pending.test.ts` covers the subtree predicate directly, including that a prefix is a path element.
- [x] 13.3 *(review round 1)* A decode failure in the image preview reports itself and offers the file, instead of leaving an empty frame with no way to get it. Verify: `FileManagerOverlay.dom.test.ts` "an image that cannot be decoded offers the file instead of an empty frame" drives the `error` event and asserts both the notice and that the download action reaches the hub.
- [x] 13.4 *(review round 1)* The browser's error-code table is checked against the hub's: `files/reasons.ts` holds it and `reasons.test.ts` reads `mapFileError` out of the Go source and asserts every code it can answer with has words. Verify: removing an entry fails the test with "the hub answers with X and the browser has no words for it" (verified by reverting).
- [x] 13.5 *(review round 1)* The reason written down for refusing a cross-root move was false — a typed destination may descend, and a link on the way may lead into another configured root, so the refusal is reachable from the browser. Verify: the corrected reason in `confine.go`, the live specification, the change-dir spec delta, `README.md`, `README.zh-CN.md` and `design.md` -- six places, all of which carried the sentence; `reasons.test.ts` is what makes the client message checked rather than dead.
- [x] 13.6 *(review round 1)* Claims corrected where the code does not do what was written: "the last call in the package on a plain path" (`design.md` and `tasks.md` 12.1 — every helper has a plain-path branch, and what is true is that none of those branches is reachable with roots configured); the unbounded wait described as transient where the marker in fact stays up (`FileManagerOverlay.vue`, `design.md` 1b); `Close`'s "safe on a service built without roots" where such a service panicked on every other method — `NewService` now substitutes an empty set, which is a behaviour change and not only a wording one. Verify: `TestAServiceBuiltWithoutRootsIsUnrestrictedRatherThanNil`.
- [x] 13.7 *(review round 1)* The app test no longer pins the refusal's wording; `ErrPathNotAllowed` is sufficient, since the path is a configured root that was listable a moment earlier. Verify: `TestShutdownReleasesTheConfiguredRoots`.

## 14. The review of the review's fixes

- [x] 14.1 *(review round 2)* `isPending` and `idle` ask from opposite ends, and the first version wrote one relation for both: a delete waits for the writes *inside* it, a save checks for operations *above* it. With a single "covers what is beneath it" predicate the wait was right and both refusals were wrong — a save of `/d/dir/f.txt` was refused by an outstanding delete of that file and not by one of `/d/dir`. Verify: `pending.test.ts` covers both directions separately, `FileManagerOverlay.dom.test.ts` "a save inside a directory being deleted is refused" fails with the one-directional predicate (found by that test, which is why it exists).
- [x] 14.2 *(review round 2)* A decode event belongs to the load that raised it. Leaving a file for another revokes its blob, which aborts the decode and raises `error` for a URL already let go of — ahead of the next fetch, so usually while the new file is loading. Unticketed, that marked the *next* file undecodable and named it in a warning. Verify: `FileManagerOverlay.dom.test.ts` "a decode failure for a file that is gone is not read against the next one" fails with the guard removed (verified by reverting).
- [x] 14.3 *(review round 2)* One cause, one answer: a handle closed by a shutdown reported `path_not_allowed` on a read and `write_failed` on the write beside it, because only the read route classifies through `classifyPathError`. `closedRootError` is now shared by all of them. Verify: `TestAClosedHandleIsNotReportedAsAWriteFailure` asserts the three classification paths agree and that an ordinary write failure is still a write failure -- it pins the helpers, not the routes; see 15.9.
- [x] 14.4 *(review round 2)* Two notices said more than the browser knows: "is being deleted" for a delete that may fail, and "was being renamed" for a file whose *directory* was. Both now describe the operation in flight rather than its outcome. Verify: the assertions in the three affected mounted tests name the new wording, so the strings cannot drift from the tests.
- [x] 14.5 *(review round 2)* Prose corrections: `tasks.md` 12.1 still carried the claim 13.6 said it had corrected; `design.md`'s replacement for it ("no operation reaches a syscall on a plain path") was false too, because the authorization walk itself runs on plain paths — `Resolve` canonicalizes and `Lstat`s them, whatever the configuration; the corrected-places count was four where six files carried the sentence; and `reasons.ts` claimed reading the Go file was the only way to hold the two halves together, where a generated artifact would also do it. Verify: read them; nothing here is machine-checkable, which is why each is written to be checkable by reading one file.

## 15. The review of the second review's fixes

- [x] 15.1 *(review round 3)* The decode guard keyed on the load and not on the event's origin, so a *late* stale `error` -- arriving after the next file had already rendered, when `shownAt` matches the live load -- was still attributed to it. The image is now keyed on its own URL and the handler checks that the event's target is the live element, so identity distinguishes what the ticket cannot. Verify: the second half of `FileManagerOverlay.dom.test.ts` "a decode failure for a file that is gone is not read against the next one" fails with the element check removed (verified by reverting).
- [x] 15.2 *(review round 3)* `idle`'s direction justification named a caller it does not have and a case that cannot arise; it now states the reason the upward direction would be dead here rather than wrong. Verify: `pending.ts`.
- [x] 15.3 *(review round 3)* The delete refusal said "A delete of f.txt is in flight" when the *directory* was what the user deleted; it now names both, as the rename notice already did. Verify: the two mounted tests assert the new wording.
- [x] 15.4 *(review round 3)* `design.md` decision 1b's own bullet list still stated the premise the paragraphs below it record as the defect — a delete asking about the tabs it is about to close "and nothing more". Verify: read 1b.
- [x] 15.5 *(review round 3)* The "only way to hold the two halves against each other" overclaim was corrected in `reasons.test.ts` and left standing in `reasons.ts` and `review.md`; both are corrected now, so the task that attributes the claim to `reasons.ts` is finally accurate. Verify: `reasons.ts`, `review.md`.
- [x] 15.6 *(review round 3)* `RootSet.Close`'s comment stated the wrong mechanism: `os.Root` refcounts the descriptor, so an operation already inside its syscall *completes* against a live fd rather than failing; only one that begins after the close is refused. The conclusion held; the mechanism did not. Verify: `guard.go`.
- [x] 15.7 *(review round 3)* `NewService`'s comment implied the composition root takes its nil-substituting branch, which it cannot — it always passes a set. Verify: `service.go`, `app.go`.
- [x] 15.8 *(review round 3)* Two things named rather than changed: a create is not ordered against an outstanding delete, unlike a save, and nothing of the user's is lost either way; and the image decode-failure path rests on a browser raising `error` for a revoked blob URL, which no test here can check because they dispatch the event themselves. Verify: `design.md`'s accepted items and residuals; the comment in `createHere`.
- [x] 15.9 *(review round 3, sharpened in round 4, **closed by this round)** The three call sites of `writeFailure` were the problem, not the mapping: each could be reverted to an inline wrap without any test noticing, because the close has to land inside a window of two adjacent syscalls and no test can put one there. The classification for the write and delete paths is now in **one place** -- deferred at the return of `Write` and of `Delete` -- so no rule of theirs is left to drop at a site (the other methods still classify where they raise, which `design.md` records as a pre-existing gap), and the sites that used to classify now return the error or wrap it in a message of their own, and the only mapping left outside the classifier is the staging file's name being too long -- which stays there because the message is the point of it. Making that possible fixed a trap in the classifier itself: it asked with `os.IsNotExist` and `os.IsPermission`, which see through a `*PathError` and nothing else, so an error wrapped on its way to the return was invisible to it -- which is why the classification had been spread out in the first place. It asks with `errors.Is` now, and a wrapped `ENOENT` is an `ENOENT`. Verify: `TestAnOperationOnAnUnwritableDirectoryIsAPermissionRefusal`, which reaches the classifier deterministically (a write into a directory that cannot be staged in) and fails if the write's defer is removed -- verified by reverting. What remains unverifiable is unchanged and now harmless: whether a close can *land* in either syscall-adjacent window is not something a test can arrange, and with one choke point there is nothing to forget if it does.

## 16. The review of the third review's fixes

- [x] 16.1 *(review round 4)* A plausible hole in the identity guard, investigated and not a hole: `release()` clears the URL before every load, so the element is torn down and a node never survives from one load to the next. The key now tracks the *load* so that the node's identity and the handler's ticket are one fact rather than two that agree by coincidence. Verify: the comment in `ImagePreview.vue` states the property of `release()` it rests on; the test written to pin the alternative was deleted because it passed with either key, and that is recorded in `review.md`.
- [x] 16.2 *(review round 4)* `tasks.md` 14.3 claimed the closed-handle test covers the three routes, which 15.9 denies fifteen lines below; the test is renamed `TestAClosedHandleIsNotReportedAsAWriteFailure` and its doc says what it pins and what it cannot. Verify: the two items now agree.
- [x] 16.3 *(review round 4)* 15.9 sharpened: the three call sites need the close to land in one of three adjacent-syscall gaps, and a handle closed with a syscall still to run before the gap fails that syscall first — so the mid-copy case the reviewer tried is not among the uncovered ones. Verify: 15.9.
- [x] 16.4 *(review round 4)* `StartDirectory` answers from a closed root set, which nothing had named. Accepted rather than changed — it acts on nothing, and every operation that touches the filesystem re-enters through `Confine`. Verify: `design.md`, accepted items.
- [x] 16.5 *(review round 4)* A decode failure is sticky until the preview is remounted, since `undecodable` is cleared only by `release()`. Accepted: the panel was true when it was set, and the preview remounts on a tab switch, which clears it. Verify: `design.md`, accepted items.
- [x] 16.6 *(review round 4)* The create's comment and `design.md` promised that "the refresh that follows both requests shows which happened" — true of the delete's refresh, which postdates the unlink, and not of the create's, which can precede it. Both now say what is and is not guaranteed. Verify: `createHere`'s comment, `design.md`.
- [x] 16.7 *(review round 4)* The specification, both READMEs and the save-refusal notice now cover the file *and a directory holding it*, which is what the upward predicate does and what the product documents had understated. Verify: the spec scenario and the READMEs' file-access sections; `openspec validate --strict`.
- [x] 16.8 *(review round 4)* A `design.md` cross-reference pointed two blocks short of the paragraph it named. Verify: read decision 1b.

## 17. The whole-change review

The first round to read the branch as one artifact rather than as the latest commit's delta. It was
given the resulting code, its seams and its merge-worthiness, and it came back with no correctness or
boundary defect -- and with one operation whose cost the change had left unbounded.

- [x] 17.1 `Delete`'s empty-directory check reads no more than it must. It answered "is this directory empty?" with the whole-directory reader, so a directory holding a hundred thousand entries cost a hundred thousand entries' worth of memory to decide that it was not empty -- the one operation left whose cost was the disk's rather than a configured limit's, and a direct counterexample to the goal `design.md` states. `dirHasEntries` reads one entry; `readDir` had no other caller and is gone. Verify: `TestDeleteRefusesANonEmptyDirectoryWithoutRecursion` and `TestDeleteRemovesFilesAndEmptyDirectories` pin the behaviour. **The cost property itself is not machine-verifiable** -- the two readers return the same answer, and only the memory they use differs.
- [x] 17.2 The record says the hub always gives its root handles back, and `cmd/visual-tmux-client` has two paths that return without `Shutdown` (a failed `Start`, and `Serve` returning on its own). **Named, not changed:** `run` returning means `main` exits and the operating system reclaims the descriptors, so the practical impact is nil -- but `RootSet.Close` justifies the whole mechanism by an embedder that outlives the process, and these are the paths it would inherit. Verify: `main.go`'s `select`, and `design.md`'s accepted items.
- [x] 17.3 The seams were re-derived rather than trusted, and hold: the staging record, the listing filter and `Confine` all key on the same canonical string; the classifier scans `[0, size)` and the transport serves `SectionReader(file, 0, size ≤ that)`, so the classification covers a superset of the bytes sent; `readListing`'s off-by-one is correct at both `limit` and `limit+1` (`File.ReadDir` returns `io.EOF` only alongside an empty slice); and read/write *follow* a final symlink while delete/rename do not, which is what each operation means. Verify: `review.md`, the seventh review.
- [x] 17.4 *(review round 5)* The browser side was read the same way: no blocking defect, and one item worth fixing rather than carrying -- `tabs.ts` named the renderer's module in a `type` import while its comment claimed to be free of the renderer, a boundary holding only because a keyword erases. `Classification` now lives in `files/classification.ts`, a leaf module. Verify: both importers, and the corrected row in the second review's closure table.
- [x] 17.5 *(review round 5)* The product-claim audit checked every quantity, key, boundary statement and `SECURITY.md` claim against the code that implements it, and swept for the wording this change superseded (finding none left). Two of its findings were this change's and are corrected: the READMEs' "transferred no further than the bound" is now stated with the condition it rests on (the hub reports the length on every read), and the clipboard notice names the condition rather than one of its causes. Verify: both READMEs, `FileManagerOverlay.vue`; `openspec validate --strict`.
- [x] 17.6 *(review round 5)* The in-app help renders Markdown without sanitizing it, where the file preview sanitizes the same input. **Named, not changed:** `HelpModal.vue` is untouched by this change, the file-manager specification's rendering requirement governs previewed *file* content and is not violated, and the guide is compiled into the binary. Verify: `design.md`'s accepted items.

## 18. The review of the whole-change round

- [x] 18.1 *(review round 6)* The module boundary the previous commit established was pinned by nothing, and it had already been broken silently once -- the second review read `tabs.ts`, saw an `import type` naming the renderer, and recorded the boundary closed. `tabs.test.ts` now reads its own module's source and fails if any import names it. Verify: the test, and `review.md`'s sixth review for why a reading is not evidence. **The limit is stated:** it checks the specifier, not the resolved graph, so a module that re-exported the renderer under another name would pass.
- [x] 18.2 *(review round 6)* The new emptiness check's rooted-and-empty path was unexercised: no test deleted an empty directory through a configured root, so a check that answered "not empty" for a rooted one would have left empty directories undeletable there with nothing failing. `TestRootedCreateAndDeleteActInsideTheRoot` now deletes one. Verify: mutating the check to always answer "not empty" fails exactly that assertion (verified by reverting).
- [x] 18.3 *(review round 6)* The blank-line gap the removed `Classification` block left. Verify: `preview.ts`. *(The item first said "two blank lines"; the block left two and the fix removed one, which the next round counted.)*

## 19. The last round

Two reviewers, given the newest commit and asked to say plainly if there was nothing. One answered
"nothing I can defend" for the Go; the other found three claims the previous round had left stale on
the browser side.

- [x] 19.1 *(review round 7)* The boundary test's stated limit was one of five. It searched for the *statement* shape, so a multi-line import -- which this repository writes sixteen times -- a side-effect `import './preview'`, a dynamic `import(...)`, and an `export ... from` all slipped past it. It now searches for the specifier, every way of naming a module names it as a string; the barrel-file limit stands and is stated. Verify: mutating `tabs.ts` to a single-line type import, a multi-line type import, and a side-effect import each fails it, and `./previewPrefs` does not (all four run).
- [x] 19.2 *(review round 7)* Three places still described the `raced` check as path-exact -- `SaveSettlement`'s doc, the comment above the check, and a test comment -- while the caller passes a query that covers the path or a directory above it, and the user-facing notice had already been reworded for the ancestor case. Verify: the three sites now match `pending.ts`'s documented direction.
- [x] 19.3 *(review round 7)* `OpenFile.size`'s doc named one of its four sources, and it is load-bearing rather than a display field: it is an argument to `presentation`, so it decides which size bound applies. Verify: the doc names all four.
- [x] 19.4 A false sentence in 9c95c63's commit message, which cannot be amended without rewriting it: it says every rooted delete "either refused or removed a symlink", where one rooted *recursive* delete in the tests succeeds -- through `removeAll`, which never reaches the emptiness check. The conclusion the sentence supports holds, and 18.2 states it precisely. Verify: `review.md`'s ninth review.

## 20. The tenth round

One reviewer, asked the two questions this branch has learned to ask of itself: is any check narrower
than it claims, and does any claim describe a superseded behaviour. It found one of each, plus a
second way past the test the previous round had just widened.

- [x] 20.1 The error-code coverage test read `mapFileError` and claimed to cover "every refusal in the file routes". `requireLocalHost` wraps every file route and answers `host_not_found`, which had no entry in the table and no test that could see it -- the same shape as the boundary test the previous round widened. The code has an entry; the clause now says what the scan reads, and the two codes that arrive from elsewhere are named by a test of their own with where each comes from. Verify: `reasons.test.ts`, the two tests.
- [x] 20.2 Two comments in the manager said "a path covers what is beneath it" while describing an `isPending` query, which looks *up*: `pending.test.ts` asserts the opposite reading directly. The effect they described was right and the mechanism was backwards, which is what a reader would have taken from them. Verify: the two comments now state the direction each call uses.
- [x] 20.3 The boundary test's stated limit was still one short: `import(/* @vite-ignore */ './preview')` matches neither pattern, and the comment is a real Vite idiom. Both patterns now allow one. Verify: `tabs.test.ts`.

## 21. The file manager review

A review of the change as a whole came back with seven findings: a data-loss path in the browser, four
ordering or accessibility defects, a request contract that was too lax to be a contract, and one claim
about the pane protocol that the code had made and could not support. Each was reproduced before it was
acted on; the last was reproduced and found to be a claim rather than a defect.

- [x] 21.1 An image is opened without being read, and a rename that gave it a name the editor would hold -- `photo.png` to `photo.txt` -- left it in the editor as an empty document, with the hub's answer for the *old* name still deciding how it was shown. Saving it replaced the picture with that emptiness. Two halves, and both are needed: `presentation` refuses to offer anything unread as text (so the editor is never handed a document nobody read, whether the read is in flight or has failed), and a rename to a name that asks for the editor reads the file, so the tab becomes what the hub says it is. Verify: `preview.test.ts` ("the editor holds source…", "presentation follows the hub…", "a name asks for the editor…") and `FileManagerOverlay.dom.test.ts` "a renamed image is read, rather than offered as an empty editor". Reverting either half fails the tests that cover it (both reverts run). This changed a rule the tests had pinned: `presentation` with a null classification used to let the name decide in both directions; the doc says why the name can decide what may be *shown* and not what may be *edited*. *(Review round 1 found two more defects in the fix itself. `readRenamedTab` identified the tab by session and path, and a path cannot tell one read of it from another -- renamed to a text name, renamed back to an image name, renamed to the text name again leaves two reads in flight for one path, and the second to land replaced the contents the first had brought, taking the user's keystrokes with them and leaving the tab reporting itself saved. What makes an answer still wanted is now the *state* it was asked about: only a tab that is still unread is filled in, so an answer for a tab that has since been read or edited is dropped. Verify: `FileManagerOverlay.dom.test.ts` "a second read of a renamed tab does not overwrite what the user typed" -- reverting the guard fails it. And the panel's reason for a file nobody read was decided by whether the name asked for the editor, which is false for a rename to a binary-extension name: `archive.zip` was described as "binary or too large to preview" when nothing had read it. The discriminator is whether the name says image, which is now `namesAnImage` in `preview.ts`. Verify: "a file renamed to a name nobody read is described as unread", and `preview.test.ts` for the predicate -- both halves fail when reverted.)*
- [x] 21.2 A read the hub has already granted goes on returning a file's contents after that file is deleted, and it can land after the delete has swept the tabs it found -- so the sweep was a moment rather than a rule, and the tab installed behind it named a path that was gone, whose save could only be answered not_found. A delete now records what it removed, for the reads that were travelling when it was answered; such a read resolves to nothing and installs no tab. `files/renames.ts` became `files/pathChanges.ts` for it: the record is of what happened to a path while a read of it travelled, and a rename is no longer the only thing that can happen. Verify: `pathChanges.test.ts` (four cases: the removal, one that predates the read, a directory's contents, and a name taken again) and `FileManagerOverlay.dom.test.ts` "a read that lands after the delete of its file installs no tab" — reverting the `recordRemoval` call fails exactly that test.
- [x] 21.3 `loadDirectory` wrote the shared listing cache before its caller could check that the load was still the current one, so a superseded answer -- a navigation and a refresh naming one directory, the header staying live throughout -- overwrote a newer one, and the tree kept rendering a directory the user had already left. The ownership rule is now the tree's rather than each caller's: a wait takes a ticket for the path it is reading, and only the newest ticket may write. Issuing it after the cache check is what keeps a call answered from the cache from retiring a wait that goes to the hub, and dropping a directory retires the waits for it so a forgotten listing cannot be put back by its own answer. Verify: `tree.test.ts` "the older of two waits for one directory does not write it", "a directory forgotten while its listing travelled does not get it back", "an answer from the cache does not retire a listing that is travelling" and "a directory invalidated and read again is written by the later read". Reverting the ticket check fails two of them; issuing the ticket before the cache check fails a third.
- [x] 21.4 `files.enabled: false` promised a capability that touches no filesystem, and resolving the configured roots at startup was a filesystem operation: a root that was missing or unreadable stopped the whole hub, terminal and sessions included. Validation no longer resolves roots while the capability is off, and the composition root builds the service without a boundary, so nothing is opened. What the service *is* in that state is stated where it is built: no roots and zero limits, which fails closed if it were ever reached, and the transport refuses every file route before it is. Verify: `TestConfig_FilesRoots` "a disabled file manager does not need a usable root" and its neighbour "an enabled file manager still needs a usable root" (which is what keeps the check skipped rather than dropped), and `TestADisabledFileManagerStartsWithUnusableRoots`. Both reverts fail the test that covers them. Both READMEs now say what is and is not touched, and the specification gained a requirement for it. *(Review round 1 of this section: the requirement root resolution is actually specified in is `runtime-configuration`, whose own words -- "SHALL reject at startup any root that does not exist" -- this change contradicted without amending, so a distinct behaviour was added to the change delta and the standing requirement qualified by enablement. And `newFileService`'s first comment claimed the disabled service "fails closed", which it does not: `Create`, `Rename` and `Delete` consult no limit and an empty root set is unrestricted by definition. The comment now says what protects the disabled path -- the transport's gate -- and `TestFileRoutesAnswerADisabledManagerWithoutReachingTheService` was widened from one route to all eight to pin it.)*
- [x] 21.5 The context menu declared `role="menu"` and behaved like a list of buttons: it never took the focus, had no arrow-key or Home/End movement, and returned the focus nowhere, so rename, delete, download, copy path and insert path were reachable only with a pointer. It now takes the focus on open, walks the actions a user can choose -- skipping the ones that are disabled rather than landing on them -- and hands the focus back to the row it was opened from. A menu the browser raised for the keyboard is anchored to that row, because such an event reports the pointer origin rather than a position that means anything. Verify: `FileManagerOverlay.dom.test.ts` "the context menu takes the focus, walks its items, and gives it back" (mounted into the document, because focus is a property of one); removing the focus-on-open fails it.
- [x] 21.6 The create route compared `kind` against the name for a directory and made everything else a file, so `{"path":"/x","kind":"directory"}` created a file and answered 204. `kind` is required and must be one of the two names the hub has, and the JSON decoder behind all three file routes now refuses an unknown field, a second value after the object, and an empty body, rather than reading a request for the parts it recognises. Verify: `TestCreateRouteRequiresAKindItKnows` and `TestFileRequestBodiesAreOneKnownObject`; reverting either the kind check or the decoder fails one of them. *(Review round 1: the wire contract was implementation-only -- the specification said nothing about what a create request carries -- so the requirement now states it, with scenarios for a kind the hub does not define, a field it does not know, and a body that is not one object.)*
- [x] 21.7 **The claim, not the defect.** `ParsePanes`' doc said a newline in a field could forge a record for another session, and named executable names and argv as the way in. Measured against a real tmux, the names cannot carry one: a session name and a window name containing a newline are refused when they are set, and `select-pane -T` leaves a title containing one as it was (silently -- what the test asserts for that one is the title). The doc now states what the parser does and does not do (the field count is validation of shape, not escaping), says the guarantee is tmux's, and records the two limits: the command field is the one a program names and the one no test covers, and a *process* whose own name carries a newline could not be produced on this machine. Verify: `TestNamesTmuxRefusesToLetCarryALineBreak` (a real server) and `TestALineBreakInAFieldWouldNotBeDetected`, which pins *both* outcomes of a break -- a break before the last field drops the pane's record and the tail takes its place, a break in the last field keeps the pane (with a truncated command) and the tail is an extra record -- so the field count is not read as a defence it is not, and the doc's account of either case can be checked rather than believed. *(Review round 1: the first correction claimed a break *always* drops the pane's record and that the last field therefore forges nothing; both halves were false and the test's second case, added here, is what shows it.)* The specification's Browser file manager and Content preview requirements gained scenarios for 21.1, 21.2 and 21.5, and `openspec validate --strict` passes.

## 22. The twelfth review of the change

Four findings and three standing tasks. The findings are a data-integrity cost the write had always had,
two accessibility gaps in the browser (a focus the manager took away, roles it declared without
behaving), and the pane-protocol measurement this branch had recorded as a residual rather than made.

- [x] 22.1 A replacement replaces the file's inode, so everything the file carried beyond its permission bits goes with it: hard links to it keep the old contents, and its owner, ACLs and extended attributes belong to the file that was replaced. That is a consequence of the atomic replace the specification requires for content safety, so it is not changed here -- the alternative, writing through the target's own inode, trades a silent metadata loss for a possible *content* loss, which is the worse of the two and the one the requirement exists to prevent. What is changed is that nothing about it is silent or unexamined: the owner is given back where the hub is allowed to set it (`takeOwner`), a target reachable under more than one name is refused until the caller agrees (the browser asks, and the retry carries the agreement), the link count is re-checked at commit because a link can be made while the body travels, and the ACL and extended-attribute cost is stated where the requirement is. Verify: `TestAWriteRefusesATargetReachableUnderOtherNames` (including that the body is never read for it, which is what makes the refusal before the transfer rather than after it), `TestAWriteRefusesANameLinkedWhileTheBodyTravelled`, `TestAReplacementKeepsTheModeAndWritesAnUnlinkedFileWithoutAsking`, `TestWriteRouteCarriesTheAgreementAboutOtherNames`, the three browser tests for the question, the retry, and the two questions a linked file that also changed on disk asks in a row -- *(added while reviewing this item: the conflict retry used to drop the agreement the first answer had given, so a linked file that also changed was asked twice and the write that finally landed was forced without carrying it. The review of the round then found the retry itself described wrongly -- as a replay of the refused request, where it saves the tab as it stands, keystrokes and all -- so the test now types while the refused write travels and asserts what the retry carries.)*, and `reasons.test.ts`'s coverage of `other_names`. Each was verified by reverting its half, including the owner: `takeOwner`'s compare and its chown are reachable as an ordinary user -- a file may be moved to a *group* the user belongs to -- and `TestAReplacementKeepsAGroupTheHubMaySet` pins exactly that, failing when the call is removed. *(Review of the round: the first version of this item claimed the whole owner path needed root and left it untested; only the cross-uid half does.)* The rooted half of the link check has a test of its own (`TestARootedWriteRefusesATargetWithOtherNames`), because the tests above all run unrooted.
- [x] 22.2 The menu handed the keyboard back to the row it was opened from, and an action that renames or removes that row takes it away again by removing the element: the browser moved the focus to the document body, and a keyboard user started over from there. Both actions now put it back -- the rename on the row the entry has now, the delete on whichever row stands where it did, measured before the action because a removed row cannot say where it was -- and only when the focus is nowhere, so a focus the user moved elsewhere is left alone. Verify: `FileManagerOverlay.dom.test.ts` "a rename and a delete leave the keyboard in the tree" (which asserts the *path* the delete lands on) and "a delete does not move the keyboard into a listing the user opened", which drives the unbounded wait, navigates during it, and asserts the focus is not placed in that listing. Removing either restore fails the first; removing the listing guard fails the second. *(Review of the round: the first test could not see the rule at all -- its fake listing kept returning the deleted name, so any restore passed -- and the rule it named was decided by a row number, which means nothing in a listing read since.)*
- [x] 22.3 The editor tabs declared `role="tablist"` and `role="tab"` without the behaviour those roles promise: no arrow keys, no roving tabindex, and no panel association. A tablist is one stop in the tab order rather than one per tab, so the strip now carries a single `tabindex="0"` on the selected tab, answers Left/Right/Home/End by moving the focus and selecting what it moved to, and names each tab so the panel can say which one it is showing (`role="tabpanel"`, labelled by the selected tab's id -- the id scheme is `tabElementId` in `files/tabs.ts`, which is where the two components can agree on it). Verify: "the editor tabs answer the keys a tablist is expected to answer" fails with the key handling removed and with a fixed tabindex (both reverts run). *(Review of the round: the strip claimed one stop in the tab order while every close button was another -- ten files meant ten stops and ten destructive controls between the user and the panel. They are `tabindex="-1"` now, Delete closes the focused tab, and the panel and the tabs name each other by id.)*
- [x] 22.4 The pane-protocol residual is measured now, and the measurement corrects the previous round -- after the review of *this* round corrected the measurement. `ParsePanes`' comment said the command field was the one field no test could reach, and that a line break in it was seen once and not reproduced. Two things are now known, and one of them is asserted on this machine: the line break is escaped (where the name had one byte the value has three -- two backslashes and an `n`) so a record is never split, and the separators are not escaped, so a value carrying them makes a record with too many fields and it is dropped whole, costing that pane its summary and no other session's summary. What is *not* measured is a process whose own name carries a line break: one cannot be made on this machine (a copy of a system binary is killed, a symlink reports its target's name, a script reports its interpreter), so what the test drives is the command string a pane was given, which is the route such a name would reach the parser through anyway. Verify: `TestACommandNameCarryingALineBreakCannotForgeARecord` asserts the escape, and that no more records come back than there are panes -- which is the half that holds whether or not the escape has the same shape where a copy of a binary runs. The record being dropped *whole* is what this platform shows, not a promise about another: the count assertion cannot see a tmux that escaped the separators too. *(Review of this round: the first version of that test and comment claimed a runnable file had been run; it had not, the pane was dead, and its skip guard was keyed on a string that survives exactly that failure. Both corrected, and decision 8 says what was and was not measured.)*
- [x] 22.5 The three standing tasks are closed rather than carried: 7.1 by a test that a rename keeps the editor (which fails with the editor keyed by path), 7.2 as a recorded accepted item because a layout measure cannot be observed in a DOM-less test, and 15.9 by making the classification one place (a deferred classify at the return of `Write` and of `Delete`) instead of three call sites each of which could be reverted unnoticed -- which also fixed the reason it had to be spread out, since the classifier asked with `os.IsNotExist` and `os.IsPermission`, neither of which sees through a wrap. Verify: `TestAnOperationOnAnUnwritableDirectoryIsAPermissionRefusal` fails when either defer is removed -- the delete half reaches `removeAll`, which is the site this round had left answering for itself (verified by reverting both) -- and no task in this file is unticked. *(Review of this round: `Delete` had no defer at all, so the claim above was false when it was written, and the recursive-delete path had been left answering as a write failure. Fixed, with the test extended to reach it.)*

## 23. The thirteenth review of the change

Three findings, two of them about what a save may touch.

- [x] 23.1 A write refused a directory and nothing else, so a target that is a named pipe, a socket or a device was *replaced* by the staged regular file: the node the caller named was gone, and no answer said so. The read path has refused every kind but regular files from the start, for the reason the read has (a pipe blocks a request goroutine, a device answers with what it produces), and the write has one of its own: the replacement is a regular file, so writing it over a pipe removes the pipe. Both kinds are now refused with the message the read path uses, and the refusal is where the dangling-link one is -- before the body travels. Verify: `TestAWriteRefusesATargetThatIsNotARegularFile`, which asserts the pipe is still a pipe afterwards and that no staging file was left; reverting the check fails it.
- [x] 23.2 The mode and the owner a replacement keeps were captured before the upload and applied after it, so a chmod made while the body travelled was undone by the save. Nothing else would notice: a chmod touches the ctime and not the modification time, so neither the observed-time check nor the identity check sees it. The metadata is now read from a stat taken after the body has arrived, and applied to the staging file
before the last check. *(Review of this round found the first version of that backwards and severe: it
left the metadata work -- a chmod, a chown, an fsync of the whole body and a close -- between the
identity check and the rename, so the two were no longer adjacent, and a second writer landing in that
tail (tens of milliseconds on a 256 MiB replacement, measured) was silently lost by both accounts. The
check runs twice now: once after the body, which is where the metadata comes from and which refuses
early, and once immediately before the rename, which is the one the specification requires to be
adjacent to it. The second is not pinned by a test and cannot be -- the window it guards is microseconds
now, where the version the reviewer caught was wide enough to hit reliably.)* Verify: `TestAReplacementUsesTheModeTheTargetHasAtCommit` tightens the mode inside the upload and asserts the replacement carries it; reverting to the capture-at-start value fails it. The owner's half of the same rule has a test of its own -- `TestAReplacementUsesTheGroupTheTargetHasAtCommit` moves the file to another group inside the upload -- which fails when the chown is fed the pre-upload observation; the earlier claim that `TestAReplacementKeepsAGroupTheHubMaySet` covered it was wrong, and the review said so.
- [x] 23.3 Closing a tab left the keyboard on the document body: the element that had the focus -- the tab when `Delete` closed it, the button beside it when a pointer did -- goes with the tab, and nothing put it back. It goes to whichever tab is *active* now -- which is not always the one that ends up first in the
strip -- and to the listing when the last one has gone: the first row when there is one, and the
listing column itself when there is not, since an empty directory renders no row and a directory
still loading renders no tree at all. *(Review of this round found two gaps here: the column had no
fallback at all, so both of those cases ended with the keyboard on the body while three places
claimed the listing took it; and the active-tab half was unpinned, since the test closed the active
tab of a two-tab strip, where the survivor is both the active tab and the first. The test now closes
a different tab with the pointer and asserts the focus lands on the active one.)* Verify: two tests. `"closing a tab puts the keyboard back on the active one"` opens three, makes the
second active, closes the third with the *pointer* -- the other way the focused element goes away --
and asserts the focus lands on the second, which is what tells an active-tab restore from a
focus-the-first-one restore; then closes the rest and asserts the focus is in the listing. *(Review of
this round: this line described the first version of that test, which closed the active tab of a
two-tab strip -- where the survivor is both the active tab and the first, so it could not tell them
apart.)* `"closing the last tab in an empty directory leaves the keyboard in the panel"` navigates to a
listing with no rows and asserts the focus lands on the listing column, which is the only test for the
fallback that case needs. Removing the restore fails both; removing the listing-column fallback fails
the second. The accepted-item note that recorded this as pre-existing is gone from `design.md`, because the strip's `Delete` is what made it this change's.

## 24. Linux CI portability findings

- [x] 24.1 Pin an existing write target with a metadata-only descriptor for the whole transfer, so Linux cannot free and reuse the inode number while `os.SameFile` is the outstanding identity check. Verify on macOS with `TestAForcedWriteRefusesAFileReplacedDuringTheTransfer`, and in Ubuntu CI where the unpinned implementation reproduced the overwrite.
- [x] 24.2 Replace the line-and-sentinel pane protocol with tmux byte-length framing for all nine fields. Verify with `TestParsePanesKeepsRecordSyntaxInsideFields`, the real-server `TestACommandNameCarryingALineBreakCannotForgeARecord`, and the Ubuntu CI case that previously produced two records for `victim`.
- [x] 24.3 Run the complete Go, race, frontend, lint, build, and strict OpenSpec checks; rebuild the `0.4.0-rc.1` candidate from the corrected commit and verify PR #10 is green.
